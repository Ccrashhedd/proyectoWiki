---
// Sidebar Navigation Component
---

<aside class="sidebar">
	<div class="sidebar-content">
		<nav class="sidebar-nav">
			<div class="nav-section">
				<h3 class="nav-section-title">Testing</h3>
				<ul class="nav-list">
					<li><a href="#niveles-prueba" class="nav-item">Niveles de prueba</a></li>
					<li><a href="#pruebas-integracion" class="nav-item">Pruebas de integración</a></li>
					<li><a href="#tipos-pruebas" class="nav-item">Tipos de pruebas</a></li>
					<li><a href="#pruebas-cambio" class="nav-item">Pruebas relacionadas con el cambio</a></li>
					<li><a href="#pruebas-no-funcionales" class="nav-item">Pruebas no funcionales</a></li>
					<li><a href="#pruebas-rendimiento" class="nav-item">Pruebas de rendimiento</a></li>
					<li><a href="#pruebas-usabilidad" class="nav-item">Pruebas de usabilidad</a></li>
				</ul>
			</div>
		</nav>
	</div>
</aside>

<style>
	.sidebar {
		position: fixed;
		left: 0;
		top: var(--header-height);
		width: var(--sidebar-width);
		height: calc(100vh - var(--header-height));
		background: var(--color-bg);
		border-right: 1px solid var(--color-border);
		overflow-y: auto;
		z-index: 50;
	}

	.sidebar-content {
		padding: 1.5rem 0;
	}

	.sidebar-nav {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.nav-section {
		padding: 0 1.5rem;
	}

	.nav-section-title {
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--color-text-secondary);
		margin-bottom: 0.75rem;
	}

	.nav-list {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.nav-sublist {
		list-style: none;
		margin: 0.25rem 0 0 0;
		padding: 0 0 0 1rem;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.nav-item {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 0.5rem 0.75rem;
		font-size: 0.875rem;
		color: var(--color-text-secondary);
		border-radius: 0.375rem;
		transition: all 0.2s;
		font-weight: 400;
	}

	.nav-item:hover {
		background: var(--color-bg-secondary);
		color: var(--color-text);
	}

	.nav-item.active {
		background: var(--color-bg-tertiary);
		color: var(--color-primary);
		font-weight: 500;
	}

	.nav-item.has-children {
		cursor: pointer;
	}

	.chevron {
		width: 14px;
		height: 14px;
		color: var(--color-text-secondary);
		transition: transform 0.2s;
	}

	.nav-item.has-children:hover .chevron {
		color: var(--color-text);
	}

	/* Scrollbar personalizado */
	.sidebar::-webkit-scrollbar {
		width: 6px;
	}

	.sidebar::-webkit-scrollbar-track {
		background: transparent;
	}

	.sidebar::-webkit-scrollbar-thumb {
		background: var(--color-border);
		border-radius: 3px;
	}

	.sidebar::-webkit-scrollbar-thumb:hover {
		background: var(--color-border-hover);
	}

	@media (max-width: 1024px) {
		.sidebar {
			transform: translateX(-100%);
			transition: transform 0.3s ease;
			box-shadow: 0 0 0 0 rgba(0, 0, 0, 0);
		}

		.sidebar.open {
			transform: translateX(0);
			box-shadow: 2px 0 8px rgba(0, 0, 0, 0.15);
		}

		.sidebar::before {
			content: '';
			position: fixed;
			top: 0;
			left: 0;
			right: 0;
			bottom: 0;
			background: rgba(0, 0, 0, 0);
			pointer-events: none;
			transition: background 0.3s ease;
			z-index: -1;
		}

		.sidebar.open::before {
			background: rgba(0, 0, 0, 0.5);
			pointer-events: auto;
		}
	}

	@media (max-width: 768px) {
		.sidebar {
			width: 280px;
		}

		.nav-section {
			padding: 0 1rem;
		}

		.nav-item {
			font-size: 0.8125rem;
			padding: 0.5rem 0.625rem;
		}
	}

	@media (max-width: 480px) {
		.sidebar {
			width: 75vw;
			max-width: 280px;
		}
	}
</style>
